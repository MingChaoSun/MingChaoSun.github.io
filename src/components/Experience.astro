---
import Section from './Section.astro';
import { experience, education } from '../data/site';
---

<Section id="experience" label={{ en: 'experience', zh: '经历' }} title={{ en: 'Experience', zh: '工作经历' }}>
  <ol class="exp">
    {
      experience.map((j) => (
        <li class="exp__item" data-reveal>
          <div class="exp__head">
            <span class="exp__org">{j.org}</span>
            <span class="exp__years mono">{j.years}</span>
          </div>
          <p class="exp__title mono">
            {j.title}
            {j.yearsNote && (
              <span class="dim"> · {j.yearsNote}</span>
            )}
          </p>
          <p class="exp__desc muted">{j.desc.en}</p>
        </li>
      ))
    }
  </ol>

  <h3 class="exp__sub mono">// education</h3>
  <ol class="exp">
    {
      education.map((e) => (
        <li class="exp__item" data-reveal>
          <div class="exp__head">
            <span class="exp__org">{e.school}</span>
            <span class="exp__years mono">{e.years}</span>
          </div>
          <p class="exp__title mono">
            {e.degree} · {e.major}{e.advisor ? ` · advised by ${e.advisor}` : ''}
          </p>
        </li>
      ))
    }
  </ol>
</Section>

<style>
  .exp {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .exp__item {
    padding-left: 1.3rem;
    border-left: 2px solid var(--border-soft);
    position: relative;
  }
  .exp__item::before {
    content: '';
    position: absolute;
    left: -6px;
    top: 0.4rem;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 3px rgba(74, 227, 181, 0.15);
  }
  .exp__head {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: baseline;
    flex-wrap: wrap;
  }
  .exp__org {
    font-weight: 600;
  }
  .exp__years {
    color: var(--accent-2);
    font-size: 0.82rem;
    flex-shrink: 0;
  }
  .exp__title {
    color: var(--accent);
    font-size: 0.88rem;
    margin-top: 0.25rem;
  }
  .exp__desc {
    font-size: 0.92rem;
    margin-top: 0.45rem;
    line-height: 1.65;
  }
  .exp__sub {
    font-family: var(--mono);
    font-size: 0.8rem;
    color: var(--text-muted);
    letter-spacing: 0.04em;
    margin: 2.2rem 0 0.4rem;
  }
</style>
